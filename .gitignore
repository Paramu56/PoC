__pycache__/
*.py[cod]

.streamlit/secrets.toml

# Local vector DB artifacts
chroma_schemes_db/

# Local/generated data artifacts
data/Karnataka Schemes.pdf
*.sqlite3
*.bin

# Local rollback snapshots (copy out if you need them elsewhere)
backup_before_source_pdf/

# MS Office lock / recovery files (never commit)
~$*
~WRL*.tmp
